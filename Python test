# Python Technical-test

### The test will be about:
* Testing your technical skills with Python

### What is about:
* General programing concepts questions(verbal)
* You will have to code a simple API end-point using Django, Flask, or FastAPI; you decide: (your development env)
  * Create an end-point that replies with a mutation of the data received,
  * Optional: pull data from with external API (we will give API doc on the call),
 

### What do you need for the test:
* Good internet connection to share screen
* A `hello world` project using Django, Flask, or FastAPI with all dependencies installed, ready to create the end-point

*Note:* There is no need to fork or clone this repository.
